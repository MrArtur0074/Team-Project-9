<UserControl x:Class="Oswalt.Views.DwgExportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Oswalt.ViewModels;assembly=Oswalt"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:DwgExportViewModel}">

	<StackPanel Margin="10">
		<Button Content="Export DWG"
                Command="{Binding ExportCommand}"
                Margin="5" />

		<TextBlock Text="Export your wing design to a DWG file."
                   Margin="5" />
	</StackPanel>
</UserControl>