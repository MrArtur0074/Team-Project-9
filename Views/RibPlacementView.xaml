<UserControl x:Class="Oswalt.Views.RibPlacementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Oswalt.ViewModels;assembly=Oswalt"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:RibPlacementViewModel}">

	<StackPanel Margin="10">
		<Button Content="Add Rib"
                Command="{Binding AddRibCommand}"
                Margin="5" />

		<Button Content="Remove Rib"
                Command="{Binding RemoveRibCommand}"
                Margin="5" />

		<Button Content="Reset Ribs"
                Command="{Binding ResetRibsCommand}"
                Margin="5" />

		<ListBox ItemsSource="{Binding Ribs.Ribs}"
                 SelectedIndex="{Binding SelectedRibIndex}"
                 Margin="5">
			<ListBox.ItemTemplate>
				<DataTemplate>
					<TextBlock Text="{Binding}" />
				</DataTemplate>
			</ListBox.ItemTemplate>
		</ListBox>
	</StackPanel>
</UserControl>