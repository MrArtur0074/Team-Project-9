<UserControl x:Class="Oswalt.Views.AirfoilConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Oswalt.ViewModels;assembly=Oswalt"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:AirfoilConfigViewModel}">

	<StackPanel Margin="10">
		<Button Margin="5"
                Command="{Binding ImportRootAirfoilCommand}"
                Content="Import Root Airfoil" />

		<Button Margin="5"
                Command="{Binding ImportTipAirfoilCommand}"
                Content="Import Tip Airfoil" />

		<TextBlock Margin="5" Text="Root Airfoil:" />
		<TextBlock Margin="5" Text="{Binding RootAirfoil.Name}" />

		<TextBlock Margin="5" Text="Tip Airfoil:" />
		<TextBlock Margin="5" Text="{Binding TipAirfoil.Name}" />
	</StackPanel>
</UserControl>